
from setuptools import setup, find_packages

setup(
    name='scripts-widget-webapp',
    version='0.0.1',
    description="Widget to display the scripts menu in OMERO.web",
    packages=find_packages(),
    keywords=['omero', 'example', 'react', 'scritpts', 'menu', 'widget'],
)